pipeline {
    agent { node env.Node }
    stages {
        stage('Code promotion') {
            steps {
                sh '''
                    aws s3 sync --delete pe_memberdna s3://memberanalytics-data-out-prod/v2/prod/code/pe_memberdna/
                '''
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}