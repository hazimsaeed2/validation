pipeline {
    agent { node env.Node }
    stages {
        stage('Code promotion') {
            steps {
                sh '''
                    aws s3 sync --delete pe_memberdna s3://memberanalytics-data-out-prod/Code_and_Data_repo/stage/${GIT_BRANCH}/code/pe_memberdna/
                '''
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}