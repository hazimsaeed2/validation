pipeline {
    agent { node env.Node }
    stages {
        stage('Reset branch') {
            steps {
                sh '''
                    cd pe_memberdna
                    git reset --hard origin/${env.GIT_BRANCH}
                    git push origin --delete sandbox
                '''
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}